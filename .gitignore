# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Local environment files. Vite inlines every VITE_* variable into the public
# bundle, so nothing secret belongs in them and none of them belong in git.
.env
.env.*
!.env.example

# Netlify CLI local state
.netlify
deno.lock

# Scratch build outputs used by local verification
.tmp-dist-*

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
.npm-cache
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
